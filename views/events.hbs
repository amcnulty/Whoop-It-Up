<div class="container-fluid events-page main-container">
    {{> header }}
    <div class="region top-region"></div>
    <div class="wrapper">
        <div class="site-logo"></div>
        <h2 class="fun-font">Events!!!</h2>
        <div class="event-section">
        {{#if events}}
            {{#each events}}
                {{> event_row}}
            {{/each}}
        {{else}}    
            <p>No event found</p>
        {{/if}}
        </div>

        <div class="controls">
            <!--<button type="button" class="btn btn-outline-primary show-btn">Show More</button>-->
        </div>
    </div>
    <div class="region bottom-region flip-bkg"></div>
</div>