<div class="container events-page main-container">
  {{> header }}
  <div class="wrapper">
    <div class="site-logo"></div>
    <h2 class="fun-font">Events!!!</h1>
    {{#each events}}
        <li>{{this.name}}</li>
        <li>{{this.date}}</li>
    {{/each}}
    <div class="controls">
      <button type="button" class="btn btn-outline-primary show-btn">Show More</button>
    </div>
  </div>
</div>